<UserControl x:Class="Kompass.Desktop.Views.ModernisierungsalternativenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Modernisierungsalternativen" FontSize="28" FontWeight="SemiBold"/>
        <TextBlock Grid.Row="1" Text="{Binding Projektname}" Margin="0,6,0,8" Foreground="#66747E" FontSize="15"/>
        <TextBlock Grid.Row="2" Text="{Binding StatusText}" Margin="0,0,0,18" Foreground="#34444F"/>
        <Border Grid.Row="3" Background="White" BorderBrush="#D5DBDF" BorderThickness="1" CornerRadius="4" Padding="18">
            <DataGrid ItemsSource="{Binding Alternativen}" AutoGenerateColumns="False" IsReadOnly="True"
                      CanUserAddRows="False" HeadersVisibility="Column" GridLinesVisibility="Horizontal">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Bezeichnung" Binding="{Binding Bezeichnung}" Width="*"/>
                    <DataGridTextColumn Header="Gesamtkosten" Binding="{Binding Gesamtkosten, StringFormat={}{0:N2} EUR}" Width="180"/>
                    <DataGridTextColumn Header="Alternative-ID" Binding="{Binding Id}" Width="260"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
