# Entwicklung

Dieses Verzeichnis enthält Hinweise für Entwicklung, Build, Tests, lokale Inbetriebnahme und Beitragsabläufe.

Änderungen müssen mindestens mit erfolgreichem `dotnet restore`, `dotnet build` und den jeweils relevanten Tests validiert werden.
