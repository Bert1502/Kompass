# LAYER_MODEL

## Domain
Aggregate, Entities, Value Objects, Invarianten, Domain Events.

## Application
Use Cases, Interfaces, Commands, Queries, DTOs, `Result` und `Result<T>`.

## Persistence
`KompassDbContext`, EF-Konfigurationen, Migrationen, Archive, Hash- und Repositoryimplementierungen.

## API
HTTP-Endpunkte, Transportmodelle und Fehlerabbildung.

## Desktop
WPF, MVVM, Navigation, API-Clients und UI-Zustand.
