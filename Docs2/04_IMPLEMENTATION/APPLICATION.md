# APPLICATION_IMPLEMENTATION

Bekannte Verträge: `IProjektService`, `IB56DateiPruefer`, `IB56HashService`, `IB56ArchivService`, `Result`, `Result<T>`. Application orchestriert Use Cases und kennt weder WPF noch konkrete EF-Konfiguration. `CancellationToken` letzter Parameter.
